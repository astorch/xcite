using System.Collections;

namespace xcite.collections {
    /// <summary> Defines a collection that publishes events when it has been modified. </summary>
    public interface IObservableCollection : ICollection, IObservableEnumerable {
        
    }
}