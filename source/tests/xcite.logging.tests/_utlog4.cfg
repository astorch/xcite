# Common properties
level = DEBUG
pattern = %date %level %text%nl
streams = cs, hs

# Console stream properties
cs = xcite.logging.streams.ConsoleStream, xcite.logging

# Hidden stream properties
hs = xcite.logging.tests.ConfigurationReaderTests+HiddenStream, xcite.logging.tests
