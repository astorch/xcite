# Common properties
level = DEBUG
pattern = %date(dd.MM.yyyy HH:mm:ss:fff) %level %text%nl
streams = fs, cs, ds

# Debug properties
ds = DebugStream

# Console properties
cs = ConsoleStream

# File properties
fs = FileStream
fs.fileName = _lfc2.txt
fs.append = true
fs.lockingModel = minimal